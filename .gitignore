# Secrets
.env

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# Data & model artifacts (large, regenerable via the pipeline)
data/raw/
data/*.parquet
*.safetensors
checkpoints/
wandb/

# Downloader checkpoint state (regenerable, but machine-specific progress)
etl/.download_checkpoint*

# Internal planning docs (kept locally, not published to GitHub).
# README.md and REPORT.md are intentionally NOT listed here — they're the
# public-facing portfolio artifacts the PRD wants visible in the repo.
PLAN.md
CLAUDE.md
legal-clause-classifier-prd.md
